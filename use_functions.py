import game
# importing the module - can use all its contents

game.rock_paper_scissors()
# calling main function, file is directly run (script)

user = game.user_conversion("R")
print(user)

computer = game.computer_conversion(2)
print(computer)
# new variables to test each function
# functions are reusable